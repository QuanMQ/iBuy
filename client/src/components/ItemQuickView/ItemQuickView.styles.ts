import styled from "styled-components";

export const DialogWrapper = styled.div`
  display: flex;

  #dialog-img {
    max-width: 40%;
    max-height: 65vh;
    object-fit: contain;
    padding: 2rem;
  }
`;
